name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build & Test (JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ '21' ]
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: maven

      - name: Build and test
        run: ./mvnw -B verify

      # 门禁跑的是**真实的预算契约矩阵**，不是"压缩率够不够高"。
      #
      # 早先这条门禁用一份只有 9 种变体的合成日志强制 ratio > 0.90 —— 那恰恰是
      # benchmarks/generate.py 开头批判的取样方式：拿高度重复的数据测出好看的数字。
      # 而且"压缩率"本身与成本只是弱相关（实测两篇论文的相关系数 r=0.15），
      # 把它当门禁会奖励"压得更狠"，而压得更狠正是丢信息的方向。
      #
      # 现在门禁直接断言这个库唯一的产品承诺：**输出 ≤ 你给的预算，或如实声明做不到**。
      - name: Verify the budget contract across the whole matrix
        run: |
          python3 benchmarks/generate.py
          python3 benchmarks/budget_matrix.py
          python3 - <<'PY'
          import io, json
          report = json.load(io.open('benchmarks/budget-matrix.json', encoding='utf-8'))
          s = report['summary']
          print("cases=%(cases)d counted=%(counted)d violated=%(violated)d "
                "declared=%(declared)d underfilled=%(underfilled)d" % s)
          assert s['violated'] == 0, "预算契约被违反 %d 次" % s['violated']
          assert s['compliance'] == 1.0, "遵守率 %.1f%%" % (s['compliance'] * 100)
          assert s['underfilled'] == 0, "有 %d 个用例几乎没把预算用上" % s['underfilled']
          # README 那张「不同预算下的压缩后 token」表，逐格复算过。
          # 它是本仓库对外引用最多的一组数字，而此前没有任何东西守着它——
          # 上面的断言只查 violated/underfilled，**从不比对任何具体数字**，
          # 所以表里的值漂了很久（8000 那格写 7,942，实测 7,920）而门禁一直绿。
          mismatches = s.get('readmeMismatches')
          assert mismatches is not None, "报告里没有 readmeMismatches 字段——这条检查没跑"
          assert mismatches == [], "README 的预算表与实测对不上：%s" % mismatches
          PY

      # 保真评测：关键信息召回 + **保真证书**（输出里不许出现原文没有的内容）。
      # 不需要模型、不需要网络、几秒跑完——这也是它敢放进 CI 的原因。
      - name: Verify fidelity (needle recall and no fabricated content)
        run: python3 benchmarks/fidelity.py

      - name: Protected content must survive at any budget
        run: |
          mkdir -p /tmp/gate
          printf '%s\n' \
            '2026-09-11 10:00:00 INFO  普通日志行 1' \
            '2026-09-11 10:00:00 INFO  普通日志行 2' \
            > /tmp/gate/pad.log
          for i in $(seq 1 2000); do
            echo "2026-09-11 10:00:00 INFO  ordinary line $i" >> /tmp/gate/pad.log
          done
          echo "2026-09-11 10:00:01 ERROR Caused by java.sql.SQLException: AML-9999 pool exhausted" >> /tmp/gate/pad.log

          java -jar ctxpress-cli/target/ctxpress.jar compress --max-tokens 200 /tmp/gate/pad.log > /tmp/gate/small.log
          grep -q "AML-9999" /tmp/gate/small.log || { echo "受保护内容被压掉了"; exit 1; }
          grep -q "Caused by" /tmp/gate/small.log || { echo "异常链被压掉了"; exit 1; }

      # README 里的测试条数必须等于真实跑出来的条数。
      #
      # 实测（2026-09-22）：那句话原先写着 98 项（core 73 + …），而真实是 102
      # （core 77）——本轮给 core 加了 4 条测试，README 没跟着改。
      # 这类数字**没有东西守着就一定会漂**：本工作区里 letterpress 漂到过 263
      # （实际 270）、mcp-sentinel 漂到过 127（实际 131）、desensitize 同一页上
      # 两处互相矛盾（110 vs 107）。有门禁的那几处就没漂。
      #
      # **位置很要紧**：它读的是上面 "Build and test" 写下的 Surefire XML。
      # 第一版我把它放进 `release-gate-selftest` job——那个 job 根本不跑
      # `./mvnw`，也没有 JDK，产物一个都没有，那一步会**每次必红**。
      # 检查放错 job 和没接线一样，都是"写了但守不住"。
      #
      # 读不到产物**就是失败**，不是跳过——否则把测试那步拿掉，这条会静默失效。
      # 它的回归用例在 `scripts/test_check_readme_test_count.py`，
      # 核心是把「这一块没量到」与「数字对不上」**分开报**——这两类的处置完全相反。
      - name: README 测试条数对账
        run: python3 scripts/check_readme_test_count.py

  release-gate-selftest:
    name: Release Gate Self-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      # 发版前那道闸（scripts/release_preflight.py）决定一个 tag 能不能变成 Release，
      # 它自己不能是没验过的。用例里每一条都对应一次真实事故或一个真实的误读风险，
      # 尤其是「查不动 != 通过」那几条：HTTP 403、响应不是数组、级别字段不认识、
      # git 历史取不到——这些在日志里的表现和"没有告警"完全一样。
      #
      # 反向验证过：拿掉 `if unknown:`、把 403 放行、拿掉 `if risky:`
      # 三处守卫分别会让 2 / 1 / 2 条用例变红。
      - name: Test the release preflight gate
        run: python3 -m unittest discover -s scripts -p 'test_*.py' -v
