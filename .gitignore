target/
.idea/
*.iml
*.ipr
*.iws
.vscode/
.DS_Store
Thumbs.db

# 基准数据由 benchmarks/generate.py 生成
benchmarks/data/

# 预算矩阵的测量结果由 benchmarks/budget_matrix.py 生成。
# 不入库：它是"当时的测量"，入库就会过期，而 README 引用的是跑出来的数字。
# CI 每次都会重跑这张矩阵当门禁。
benchmarks/budget-matrix.json

# 保真评测的测量结果同样是当时的测量，脚本与结论在 README 里
benchmarks/fidelity-report.json
