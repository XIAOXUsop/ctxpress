<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.xiaoxusop</groupId>
        <artifactId>ctxpress-parent</artifactId>
        <version>0.3.1</version>
    </parent>

    <artifactId>ctxpress-cli</artifactId>
    <name>ctxpress-cli</name>
    <description>命令行：直接在管道里压缩日志/JSON/文本，无需写代码</description>

    <dependencies>
        <!--
          CLI 默认带真实词表：命令行用户拿到的是一个可执行 jar，
          没有"再加一个依赖"的机会，而"预算 8000"如果按乐观估算算，实际会撑爆窗口。
          代价是 fat jar 从约 2.4MB 涨到约 5.6MB（jtokkit 自带 BPE 词表）。
          想退回启发式估算，用 tokenizer 选项选 heuristic。
        -->
        <dependency>
            <groupId>io.github.xiaoxusop</groupId>
            <artifactId>ctxpress-tokenizer-jtokkit</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.xiaoxusop</groupId>
            <artifactId>ctxpress-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>ctxpress</finalName>
        <plugins>
            <!-- 打成可执行 fat jar：java -jar ctxpress.jar 即可用，无需管理 classpath -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.xiaoxusop.ctxpress.cli.Main</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
